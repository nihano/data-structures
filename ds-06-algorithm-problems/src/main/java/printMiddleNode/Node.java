package printMiddleNode;

public class Node {
    int id;
    Node next; //we are referring to another elements address.default value will be null.

    public Node(int id) {
        this.id = id;
    }
}
