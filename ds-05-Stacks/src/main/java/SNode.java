public class SNode<T> {
    public T value; //data
    SNode next;

    public SNode(T value) {
        this.value = value;
    }
}

   /*SNode=> StackNode, we are using LinkedList structure
   in the backbone so Node class created
    */